Roadmap for code tests.

Indicate commands to test to verify the stability of the code.

Example: easy: balance (). (Works, tests performed 140).

Cambiar la fecha en la que se encuenta un bloque por algo mas entendible. (Hecho)

Realizar automatizacion del nodo.()

Traducir el codigo a Español y explicarlo.()

Saber el valor del bloque en el mismo momento cuando se descubre,es una posivilidad. ()
